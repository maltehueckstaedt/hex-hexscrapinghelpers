linters: 
  linters_with_defaults(
    line_length_linter(1250),  # setze Zeilenlaenge auf 250
    object_usage_linter = NULL, # Deaktiviere den Linter fuer Funktionsdefinitionen
    trailing_whitespace_linter(allow_empty_lines = TRUE) # Entferne trailing whitespace
  )
encoding: "UTF-8"
